---
import DualCircleLoader from "../DualCircleLoader.astro";

const serviceId = import.meta.env.PUBLIC_EMAILJS_SERVICE_ID;
const templateId = import.meta.env.PUBLIC_EMAILJS_TEMPLATE_ID;
const publicKey = import.meta.env.PUBLIC_EMAILJS_PUBLIC_KEY;
---

<div class="relative h-full">
  <div class="absolute inset-0 bg-gradient-to-br from-blue-600/10 to-purple-600/10 rounded-2xl blur-xl"></div>
  <div class="relative dark:bg-gray-800/50 dark:backdrop-blur-sm p-8 rounded-2xl dark:border border-gray-700/50 shadow-2xl bg-[#f7f8f9] h-full flex flex-col">
    <h3 class="text-3xl font-semibold mb-2 bg-gradient-to-r from-blue-400 to-purple-400 bg-clip-text text-transparent">Send a Message</h3>
    <p class="dark:text-gray-300 text-[#4c5664] mb-6">下記のフォームにご記入いただければ、すぐにご返信いたします</p>
    <form id="contact-form" class="space-y-4 flex-grow flex flex-col relative" novalidate>
      <div id="loader" class="hidden absolute inset-0 flex items-center justify-center bg-white/70 dark:bg-gray-900/70 z-50">
        <DualCircleLoader />
      </div>

      <input name="user_name" type="text" placeholder="お名前" required
        class="w-full p-3 rounded-md text-black dark:text-white placeholder-gray-400 outline-none dark:bg-gray-700 bg-white border border-gray-400 focus:border-blue-500 focus:ring-blue-500 focus-visible:ring-offset-2 focus-visible:ring-2 dark:focus-visible:ring-offset-gray-700" />

      <input name="user_email" type="email" placeholder="メールアドレス" required
        class="w-full p-3 rounded-md text-black dark:text-white placeholder-gray-400 outline-none dark:bg-gray-700 bg-white border border-gray-400 focus:border-blue-500 focus:ring-blue-500 focus-visible:ring-offset-2 focus-visible:ring-2 dark:focus-visible:ring-offset-gray-700" />

      <textarea name="message" rows="4" placeholder="メッセージ" required
        class="w-full p-3 rounded-md text-black dark:text-white placeholder-gray-400 outline-none dark:bg-gray-700 bg-white border border-gray-400 focus:border-blue-500 focus:ring-blue-500 focus-visible:ring-offset-2 focus-visible:ring-2 flex-grow dark:focus-visible:ring-offset-gray-700"></textarea>

      <button type="submit"
        class="w-full py-3 bg-gradient-to-r from-blue-600 to-purple-600 text-white font-semibold rounded-md hover:opacity-90 transition">メッセージを送信</button>
    </form>
  </div>

  <!-- EmailJS -->
  <script src="https://cdn.jsdelivr.net/npm/emailjs-com@3/dist/email.min.js"></script>
  <script define:vars={{ serviceId, templateId, publicKey }} is:inline>
    window.addEventListener("DOMContentLoaded", () => {
      emailjs.init(publicKey);
      const form = document.getElementById("contact-form");
      const loader = document.getElementById("loader");

      form.addEventListener("submit", (e) => {
        e.preventDefault();

        const emailInput = form.querySelector('input[name="user_email"]');
        const email = emailInput.value.trim();
        const emailPattern = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;

        if (!emailPattern.test(email)) {
          alert("有効なメールアドレスを入力してください。");
          emailInput.focus();
          return;
        }

        loader.classList.remove("hidden");
        emailjs.sendForm(serviceId, templateId, form).then(() => {
          alert("メッセージが正常に送信されました！");
          form.reset();
        }).catch((error) => {
          alert("メッセージの送信に失敗しました: " + (error.text || "不明なエラー"));
        }).finally(() => {
          loader.classList.add("hidden");
        });
      });
    });
  </script>
</div>
